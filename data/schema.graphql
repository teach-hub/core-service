schema {
  query: RootQueryType
  mutation: RootMutationType
}

"""Root mutation"""
type RootMutationType {
  """Updates a user"""
  updateUser(file: String, githubId: String, lastName: String, name: String, notificationEmail: String, userId: ID!): User
}

"""Root query"""
type RootQueryType {
  findUser(userId: ID): User

  """Logged in user"""
  viewer: ViewerType
}

"""A subject within TeachHub"""
type Subject {
  active: Boolean
  code: String!
  id: ID
  name: String!
}

"""A non-admin user within TeachHub"""
type User {
  file: String
  githubId: String
  lastName: String
  name: String
  notificationEmail: String
  userId: ID
}

"""Courses belonging to viewer"""
type ViewerCourseType {
  active: Boolean!
  name: String!
  organization: String!
  period: Int!

  """Role that user has within a course"""
  role: ViewerRoleType!

  """Subject the course belongs to"""
  subject: Subject!
  year: Int!
}

type ViewerRoleType {
  active: Boolean
  id: Int
  name: String
  parent: ViewerRoleType
  permissions: [String]
}

type ViewerType {
  active: Boolean
  courses: [ViewerCourseType]!
  file: String!
  githubId: String!
  lastName: String!
  name: String!
  notificationEmail: String!
  userId: String!
}
